git log
git add
git status
git commit -m "teste"
git init
git config --global user.nome "Murillo"
git config --global user.email "murillo_mcrc@hotmail.com"
git config --global color.ui auto
git checkout -b getname
git push -u origin master
$ git remote add origin https://github.com/muloko/minicuro_git.git
